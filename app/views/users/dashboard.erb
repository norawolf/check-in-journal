<h1>Welcome, <%= current_user.username %></h1>

<div class="row">
  <div class="u-full-width" style="text-align: center; padding-bottom: 50px;">
    <% if current_user.entries.count > 0 %>
      <p>Your last entry was on <%= current_user.entries.last.date.strftime('%B %d, %Y')%>.</p>
    <% else %>
      It looks like you haven't created any entries yet.
      <% end %>
  </div>
</div>

<div class="row">
  <div class="six columns" style="text-align:center;">
    <a class="button button-primary" href="/entries/new">Create a new entry</a>
  </div>
  <div class="six columns" style="text-align:center;">
    <a class="button button-primary"href="/entries">See all your entries</a>
  </div>
</div>

<h1>edit this entry</h1>

<form action="/entries/<%= @entry.id %>" method="post">
  <input id="hidden" type="hidden" name="_method" value="patch">
  <label>Date:</label>
  <input type="date" name="entry[date]" value="<%= @entry.date.strftime('%Y-%m-%d') %>"><p>

   How are you feeling?<br>
   <label>Select a mood from the list:</label><p>

   <select multiple name="entry[moods][]">
     <% @moods.each do |mood| %>
       <option name="<%= mood.name %>"><%= mood.name %></option>
     <% end %>
   </select><p>

   and/or, add a different mood<br>
   <label>I'm feeling</label>:
   <input type="text" name="entry[moods][]" value=""><p>


   <label>What did you do today?</label><br>
   <% @activities.each do |activity| %>
     <input type="checkbox" name="entry[activities][]" <%='checked' if @entry.activities.include?(activity) %>> <%= activity.name %></input>
   <% end %> <p>

   <label>Add another activity:</label><br>
   <input type="text" name="entry[activities][]"><p>

   <label>Add a note about your day:</label><p>
   <input type="text" name="entry[note]"><p>


   <input type="submit" value="Update Entry">
</form>
